# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(pressure_paroscientificTaskLib)
ADD_LIBRARY(${PRESSURE_PAROSCIENTIFIC_TASKLIB_NAME} SHARED 
    ${PRESSURE_PAROSCIENTIFIC_TASKLIB_SOURCES})

add_dependencies(${PRESSURE_PAROSCIENTIFIC_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${PRESSURE_PAROSCIENTIFIC_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PRESSURE_PAROSCIENTIFIC_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PRESSURE_PAROSCIENTIFIC_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PRESSURE_PAROSCIENTIFIC_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PRESSURE_PAROSCIENTIFIC_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PRESSURE_PAROSCIENTIFIC_TASKLIB_HEADERS}
    DESTINATION include/orocos/pressure_paroscientific)

